# Strapi-Instanz, aus der zur Build-Zeit gelesen wird
STRAPI_URL=http://localhost:1337

# Nur noetig, wenn die Public Role keinen Lesezugriff hat (aktuell nicht der Fall)
# STRAPI_TOKEN=

# Oeffentliche Adresse der Seite — fuer Sitemap, RSS und canonical-Tags
SITE_URL=http://localhost:4321
